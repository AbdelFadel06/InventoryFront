# VITE_API_URL=http://localhost:8000/api


VITE_API_URL=https://inventoryback-production.up.railway.app/api

# Cloudinary — upload direct depuis le navigateur
# 1. Créer un compte sur https://cloudinary.com (gratuit)
# 2. Dashboard → Settings → Upload → Add upload preset → Mode: Unsigned
# 3. Copier le Cloud name (dashboard) et le preset name ici

VITE_CLOUDINARY_CLOUD_NAME=dbbw74aip
VITE_CLOUDINARY_UPLOAD_PRESET=CuisineStore


#  "scripts": {
#     "dev": "vite",
#     "build": "tsc -b && vite build",
#     "lint": "eslint .",
#     "preview": "vite preview"
#   },
